name: paytm_payments
description: Paytm Payments Flutter Android SDK. Setup up staging and production environment one after the other with very easy to use function call.
version: 0.3.1
author: Apoorv Jain <ajax8732@gmail.com>
homepage: https://github.com/apoorv9496/paytm_payments

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^0.12.0+2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    androidPackage: xyz.ajax.paytm_payments
    pluginClass: PaytmPaymentsPlugin